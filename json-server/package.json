{
  "name": "json-server-users",
  "version": "1.0.0",
  "description": "Mock user data server for route loader demo",
  "scripts": {
    "dev": "json-server --watch db.json --port 3001",
    "start": "json-server db.json --port 3001 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
